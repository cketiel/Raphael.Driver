<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Raphael.Driver.ViewModels"
             x:Class="Raphael.Driver.Views.PullOutDetailPage"
             x:DataType="viewmodels:PullOutDetailPageViewModel"
             xmlns:converters="clr-namespace:Raphael.Driver.Converters"
             xmlns:controls="clr-namespace:Raphael.Driver.Controls"
             Title="{Binding PageTitle}"
             BackgroundColor="{StaticResource PageBackgroundColor}">

    <Shell.TitleView>
        <controls:PageTitleBar Title="{Binding PageTitle}" Icon="&#xf1b9;" ShowBell="False" />
    </Shell.TitleView>

    <Shell.BackButtonBehavior>
        <BackButtonBehavior Command="{Binding GoBackCommand}" />
    </Shell.BackButtonBehavior>

    <ContentPage.Resources>

        <converters:TimeSpanToDateTimeConverter x:Key="TimeSpanToDateTimeConverter" />
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <!-- <converters:ScheduleColorConverter x:Key="ScheduleColorConverter" /> -->

        <Style TargetType="Grid" x:Key="NavRowStyle">
            <Setter Property="BackgroundColor" Value="{StaticResource CardBackgroundColor}"/>
        </Style>
        
        <Style TargetType="Label" x:Key="NavLabelStyle">
            <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalOptions" Value="Center"/>
        </Style>
        <Style TargetType="Label" x:Key="NavIconStyle">
            <Setter Property="FontFamily" Value="FontAwesomeSolid"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalOptions" Value="Center"/>
            <Setter Property="HorizontalOptions" Value="Start"/>
            <Setter Property="WidthRequest" Value="30"/>
        </Style>
        <Style TargetType="Label" x:Key="DetailHeaderLabel">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextColor" Value="{StaticResource SecondaryTextColor}"/>
        </Style>
        <Style TargetType="Label" x:Key="DetailValueLabel">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>
        <!-- Style for dividers inside the details card -->
        <Style TargetType="BoxView" x:Key="DetailSeparatorStyle">
            <Setter Property="HeightRequest" Value="1" />
            <Setter Property="Color" Value="{StaticResource SeparatorColor}" />
            <Setter Property="HorizontalOptions" Value="Fill" />
        </Style>
    </ContentPage.Resources>

    <Grid>
    
    <VerticalStackLayout Spacing="3">

        <Grid ColumnDefinitions="7,*" Style="{StaticResource NavRowStyle}" IsVisible="{Binding IsPrimaryActionVisible}">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding OdometerOrPerformActionCommand}"/>
            </Grid.GestureRecognizers>
           
            <BoxView Grid.Column="0" 
                     Color="{Binding EventColor}"/>

            <Grid Grid.Column="1" Padding="0" >

                    <!-- 1. ODOMETER (Visible if not entered) -->
                    <Grid ColumnDefinitions="Auto, *, Auto" IsVisible="{Binding ShowOdometerAction}" Padding="15">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding OdometerOrPerformActionCommand}"/>
                        </Grid.GestureRecognizers>
                        <Label Grid.Column="0" Text="" Style="{StaticResource NavIconStyle}"/>
                        <Label Grid.Column="1" Text="Odometer" Style="{StaticResource NavLabelStyle}"/>
                        <Label Grid.Column="2" Text="" FontFamily="FontAwesomeSolid" VerticalOptions="Center"/>
                    </Grid>

                    <!-- 2. SIGNATURE (Only for Pull-in after Odometer) -->
                    <Grid ColumnDefinitions="Auto, *, Auto" IsVisible="{Binding ShowSignatureAction}" Padding="15">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToSignatureCommand}"/>
                        </Grid.GestureRecognizers>
                        <Label Grid.Column="0" Text="✍" Style="{StaticResource NavIconStyle}"/>
                        <Label Grid.Column="1" Text="Driver Signature" Style="{StaticResource NavLabelStyle}"/>
                        <Label Grid.Column="2" Text="" FontFamily="FontAwesomeSolid" VerticalOptions="Center"/>
                    </Grid>

                    <!-- 3. PERFORM (Visible when previous steps are completed) -->
                    <Grid ColumnDefinitions="Auto, *, Auto" IsVisible="{Binding ShowPerformAction}" Padding="15">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding OdometerOrPerformActionCommand}"/>
                        </Grid.GestureRecognizers>
                        <Label Grid.Column="0" Text="" Style="{StaticResource NavIconStyle}"/>
                        <Label Grid.Column="1" Text="Perform" Style="{StaticResource NavLabelStyle}"/>
                        <Label Grid.Column="2" Text="" FontFamily="FontAwesomeSolid" VerticalOptions="Center"/>
                    </Grid>
                
                    <!-- VIEW FOR "ODOMETER" STATUS -->
                <!-- Displayed when IsOdometerEntered is false -->
                <!--<Grid ColumnDefinitions="Auto, *, Auto" 
                      IsVisible="{Binding IsOdometerEntered, Converter={StaticResource InvertedBoolConverter}}">
                    <Label Grid.Column="0" Text="" Style="{StaticResource NavIconStyle}"/>
                    <Label Grid.Column="1" Text="Odometer" Style="{StaticResource NavLabelStyle}"/>
                    <Label Grid.Column="2" Text="" FontFamily="FontAwesomeSolid" VerticalOptions="Center"/>
                </Grid>-->

                <!-- VIEW FOR THE "PERFORM" STATUS -->
                <!-- Displayed when IsOdometerEntered is true -->
                <!--<Grid ColumnDefinitions="Auto, *, Auto"
                      IsVisible="{Binding IsOdometerEntered}">
                    <Label Grid.Column="0" Text="" Style="{StaticResource NavIconStyle}"/>
                    <Label Grid.Column="1" Text="Perform" Style="{StaticResource NavLabelStyle}"/>
                    <Label Grid.Column="2" Text="" FontFamily="FontAwesomeSolid" VerticalOptions="Center"/>
                </Grid>-->

            </Grid>

            <!--<Grid Grid.Column="1" Padding="15,15,15,15" ColumnDefinitions="Auto, *, Auto">
                <Label Grid.Column="0" Text="" Style="{StaticResource NavIconStyle}"/>
                <Label Grid.Column="1" Text="Odometer" Style="{StaticResource NavLabelStyle}"/>
                <Label Grid.Column="2" Text="" FontFamily="FontAwesomeSolid" VerticalOptions="Center"/>
            </Grid>-->
        </Grid>

        <Grid ColumnDefinitions="7,*" Style="{StaticResource NavRowStyle}">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding GoToMapsCommand}"/>
            </Grid.GestureRecognizers>
           
            <BoxView Grid.Column="0" 
                     Color="{Binding EventColor}"/>

            <Grid Grid.Column="1" Padding="15,15,15,15" ColumnDefinitions="Auto, *, Auto">
                <Label Grid.Column="0" Text="" Style="{StaticResource NavIconStyle}"/>
                <Label Grid.Column="1" Text="{Binding MapActionText}" Style="{StaticResource NavLabelStyle}"/>
                <Label Grid.Column="2" Text="" FontFamily="FontAwesomeSolid" VerticalOptions="Center"/>
            </Grid>
        </Grid>

        <!-- Details Section -->
        <Label Text="DETAILS" Margin="15,20,15,5" FontSize="14" TextColor="Gray" HorizontalOptions="Center" FontAttributes="Bold"/>

            <Border StrokeThickness="0" BackgroundColor="{StaticResource CardBackgroundColor}" Padding="15" Margin="10,0"
                StrokeShape="RoundRectangle 5">
            <VerticalStackLayout Spacing="0">
                <!-- Address -->
                <VerticalStackLayout Padding="0,10">
                    <Grid>
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding CopyAddressCommand}"/>
                        </Grid.GestureRecognizers>
                        <Label Style="{StaticResource DetailHeaderLabel}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="ADDRESS "/>
                                    <Span Text="(Tap to Copy)" TextColor="#1E90FF"/>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </Grid>
                    <Label Text="{Binding Event.Address}" Style="{StaticResource DetailValueLabel}" LineBreakMode="WordWrap"/>
                </VerticalStackLayout>

                <BoxView Style="{StaticResource DetailSeparatorStyle}" />

                <!-- Event Type -->
                <VerticalStackLayout Padding="0,10">
                    <Label Text="EVENT TYPE" Style="{StaticResource DetailHeaderLabel}"/>
                    <Label Text="{Binding Event.Name}" Style="{StaticResource DetailValueLabel}"/>
                </VerticalStackLayout>

                <BoxView Style="{StaticResource DetailSeparatorStyle}" />
                
                <!-- Scheduled Time -->
                <VerticalStackLayout Padding="0,10">
                    <Label Text="SCHEDULED TIME" Style="{StaticResource DetailHeaderLabel}"/>
                    <Label Text="{Binding Event.Pickup, Converter={StaticResource TimeSpanToDateTimeConverter}, StringFormat='{0:hh:mm tt}'}" Style="{StaticResource DetailValueLabel}"/>
                </VerticalStackLayout>

                <BoxView Style="{StaticResource DetailSeparatorStyle}" />
                
                <!-- ETA -->
                <VerticalStackLayout Padding="0,10">
                    <Label Text="ETA" Style="{StaticResource DetailHeaderLabel}"/>
                    <Label Text="{Binding Event.ETA, Converter={StaticResource TimeSpanToDateTimeConverter}, StringFormat='{0:hh:mm tt}'}" Style="{StaticResource DetailValueLabel}"/>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </Border>
    </VerticalStackLayout>
    
    <ActivityIndicator IsRunning="{Binding IsBusy}"
                       IsVisible="{Binding IsBusy}"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       Color="{StaticResource Primary}"/>

    <!-- The van that will not start is asked about here, before the Pull-out is performed. -->
    <controls:CallRequestButton VehicleRouteId="{Binding Event.VehicleRouteId}"
                                ScheduleId="{Binding Event.Id}" />

    </Grid>
</ContentPage>